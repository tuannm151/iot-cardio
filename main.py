# from db import DBConnection
# conn = DBConnection()

# # insert to table tblSKLearn with columns: id, algo, predict, datetime
# conn.execute("INSERT INTO tblSKLearn (algo, predict, datetime) VALUES ('lg',71.1234,1670594984)".format(
#     "algo", "predict", "datetime"))
